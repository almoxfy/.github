# Aplica `type: improvement` a partir do dropdown "Tipo de tarefa" quando for
# refatoração. Documentação / CI usam Issue types (Documentation / DevOps / Misc).

name: Issue — label task by kind

on:
  issues:
    types: [opened]

jobs:
  apply-type-label:
    if: contains(github.event.issue.body, '### Tipo de tarefa')
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            const body = context.payload.issue.body || '';
            const header = /^### Tipo de tarefa\s*$/m;
            const hm = body.match(header);
            if (!hm) return;

            const start = hm.index + hm[0].length;
            const slice = body.slice(start);
            const next = slice.search(/\n### /);
            const block = next === -1 ? slice : slice.slice(0, next);
            const lines = block
              .split(/\r?\n/)
              .map((l) => l.trim())
              .filter((l) => l.length > 0);
            const kind = lines[0];
            if (!kind) {
              console.log('::warning::Seção Tipo de tarefa estava vazia');
              return;
            }

            const map = {
              'Refatoração / melhoria interna': 'type: improvement',
            };
            const label = map[kind];
            if (!label) {
              console.log(`Nenhuma label automática para: ${kind} (usar Issue type Documentation / DevOps / Misc)`);
              return;
            }

            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.issue.number,
              labels: [label],
            });
